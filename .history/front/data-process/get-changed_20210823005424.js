import { query, queryAll, show } from '../utils/query-helpers'
import { button, element, input, select } from '../data-process/queries'

// СОБИРАЕМ ОТРЕДАКТИРОВАННЫЕ В ПАНЕЛИ ДАННЫЕ

class ChangedData {
   constructor(params) {
      this.params = params
   }
   get collect() {

   }

   inputFields() {

   }
}

export { ChangedData }